# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Test MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/chat_app_test

# Test PostgreSQL Configuration  
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=chat_app_test
POSTGRES_CONNECTION_STRING=postgresql://postgres:postgres@postgres:5432/chat_app_test?sslmode=disable

# Redis Configuration (if you decide to use it later)
REDIS_URI=redis://localhost:6379